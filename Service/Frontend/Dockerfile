FROM node:20.15.1

ARG VITE_API_URL
ENV VITE_API_URL=${VITE_API_URL}
ARG VITE_WS_URL
ENV VITE_WS_URL=${VITE_WS_URL}
ARG VITE_IDLE_TIME
ENV VITE_IDLE_TIME=${VITE_IDLE_TIME}

WORKDIR /app
COPY . .
RUN npm install && npm run build
